{
  "name": "allus-ai-website-local",
  "private": true,
  "scripts": {
    "check": "npm run check:syntax && npm run lint && npm test",
    "check:syntax": "node --check app.js && node --check serve.cjs",
    "lint": "node scripts/site-check.cjs",
    "start": "node serve.cjs",
    "test": "node --test tests/*.test.cjs"
  }
}
